name: Deploy to GitHub Pages

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Duplicate workflow disabled. Use Deploy React site to GitHub Pages instead."
